extends layout

block content
  #messageBox!=Message
  if locals.images
    each image in images
      .imgbox
        .img
          a(href=image.link)
            img.bgg_img(src=image.media.m)

    
